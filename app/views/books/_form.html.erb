<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, 'error') %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t(:author) %><br>
    <%= f.text_field :author %>
  </div>
  <div class="field">
    <%= f.label t(:title) %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label t(:description) %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label t(:image) %> <%= t(:image_help)%><br>
    <%= f.text_area :image, onchange: 'image_add_preview()' %>
    <div class="image_preview">

    </div>
  </div>
  <div class="actions">
    <%= f.submit(t(:submit)) %>
  </div>
<% end %>
